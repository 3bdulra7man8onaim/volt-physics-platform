# 🚀 البدء السريع - منصة الفولت في الفيزياء

## ⚡ التشغيل السريع

### 1. التطوير المحلي
```bash
# نسخ المشروع
git clone https://github.com/your-username/volt-physics-platform.git
cd volt-physics-platform

# تشغيل بيئة التطوير
./deploy.sh dev
```

### 2. الإنتاج
```bash
# نشر للإنتاج
./deploy.sh prod

# نشر مع SSL
./deploy.sh prod --ssl
```

## 🔗 الروابط المهمة

### بيئة التطوير
- **الموقع الرئيسي**: http://localhost:3000
- **API**: http://localhost:5000
- **قاعدة البيانات**: http://localhost:8080 (phpMyAdmin)
- **البريد الإلكتروني**: http://localhost:8025 (MailHog)

### بيئة الإنتاج
- **الموقع الرئيسي**: https://volt-physics.com
- **لوحة المراقبة**: http://localhost:9090
- **فحص الصحة**: https://volt-physics.com/health

## 👤 حسابات تجريبية

### المدرس
- **البريد**: mr.sayed@volt-physics.com
- **كلمة المرور**: password123

### المدير
- **البريد**: admin@volt-physics.com
- **كلمة المرور**: admin123

### طالب تجريبي
- **البريد**: ahmed@example.com
- **كلمة المرور**: student123

## 📱 الميزات الرئيسية

### للطلاب
- ✅ تسجيل الدخول والاشتراك
- ✅ مشاهدة الفيديوهات التعليمية
- ✅ حل الاختبارات والواجبات
- ✅ تتبع التقدم الدراسي
- ✅ استخدام الكوبونات

### للمدرسين
- ✅ إدارة الكورسات والمحاضرات
- ✅ رفع الفيديوهات
- ✅ إنشاء الاختبارات
- ✅ متابعة أداء الطلاب
- ✅ إرسال الإشعارات

### للمديرين
- ✅ إدارة شاملة للمنصة
- ✅ إحصائيات مفصلة
- ✅ إدارة المستخدمين
- ✅ إعدادات النظام
- ✅ النسخ الاحتياطية

## 🛠️ أوامر مفيدة

```bash
# فحص صحة التطبيق
./deploy.sh health

# إنشاء نسخة احتياطية
./deploy.sh backup

# تنظيف الملفات القديمة
./deploy.sh cleanup

# عرض السجلات
docker-compose logs -f

# إعادة تشغيل خدمة معينة
docker-compose restart volt-web

# الدخول لحاوية معينة
docker exec -it volt-physics-web bash
```

## 🔧 إعدادات سريعة

### تغيير كلمات المرور
```bash
# تحرير ملف البيئة
cp .env.example .env
nano .env

# تحديث كلمات المرور في:
# - DB_PASSWORD
# - JWT_SECRET
# - MAIL_PASSWORD
```

### إضافة دومين جديد
```bash
# تحرير nginx.conf
nano nginx.conf

# إضافة server_name الجديد
server_name volt-physics.com www.volt-physics.com your-domain.com;
```

### تفعيل HTTPS
```bash
# إضافة شهادات SSL
mkdir -p nginx/ssl
# نسخ الشهادات إلى nginx/ssl/

# إعادة تشغيل nginx
docker-compose restart volt-nginx
```

## 📊 مراقبة الأداء

### مؤشرات مهمة
- **استخدام المعالج**: < 70%
- **استخدام الذاكرة**: < 80%
- **مساحة القرص**: > 20% متاحة
- **زمن الاستجابة**: < 2 ثانية

### أوامر المراقبة
```bash
# مراقبة الموارد
docker stats

# فحص السجلات
docker-compose logs --tail=100 -f

# فحص قاعدة البيانات
docker exec volt-physics-db mysql -u volt_user -p -e "SHOW PROCESSLIST;"
```

## 🆘 حل المشاكل الشائعة

### المنفذ مستخدم
```bash
# إيقاف الخدمات المتضاربة
sudo lsof -i :3000
sudo kill -9 PID_NUMBER
```

### مشكلة في قاعدة البيانات
```bash
# إعادة تشغيل قاعدة البيانات
docker-compose restart volt-db

# استعادة من نسخة احتياطية
docker exec -i volt-physics-db mysql -u volt_user -p volt_physics < backup.sql
```

### مشكلة في الذاكرة
```bash
# تنظيف Docker
docker system prune -a

# إعادة تشغيل الخدمات
docker-compose down && docker-compose up -d
```

## 📞 الدعم الفني

### المطور
- **الاسم**: Abdulrahman Ghonaim
- **البريد**: abdulrahman.ghonaim@gmail.com
- **واتساب**: +20 XXX XXX XXXX

### المدرس
- **الاسم**: مستر السيد محمد
- **البريد**: mr.sayed@volt-physics.com
- **هاتف**: +20 XXX XXX XXXX

---

**⚡ منصة الفولت في الفيزياء - جاهزة للانطلاق!**